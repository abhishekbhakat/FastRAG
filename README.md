# FastAPI File Upload and Query Application

This is a basic FastAPI application with file upload and simple querying capabilities.

## Setup

1. Install dependencies:

2. Run
```
uvicorn fastrag.main:app --host 0.0.0.0 --port 3000
```